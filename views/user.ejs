<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="bg-zinc-900 text-white max-w-[80%] mx-auto">
    <div class="px-10 mt-4">
        <a href="/" class="text-sm opacity-60">Back to Home</a>
    </div>
    <h1 class="px-10 pt-4 text-2xl text-white/70 font-semibold">All Users</h1>
    <div class="md:p-10 py-4 flex flex-wrap gap-5">
        <%if(user.length>0) { %>
          <% user.forEach((user) => { %>
                    <div class="w-[300px] min-h-[300px] bg-zinc-800 rounded-md">
                        <div class="w-full h-60 rounded-md bg-zinc-300">
                            <img src="<%= user.image %>" class="w-full h-60 object-fit" alt="user">
                        </div>
                        <h1 class="mt-3 px-3 text-white/90 text-xl font-semibold">
                            <%= user.name %>
                        </h1>
                        <h3 class="mt-2 px-3 text-white/60 text-md">
                            <%= user.email %>
                        </h3>
                        <div class="w-full flex gap-5 mt-5 py-3">
                            <a href="/update/<%= user._id %>" class="text-lg px-3 font-semibold">Update</a>
                            <a href="/delete/<%= user._id %>" class="text-red-500 text-lg font-semibold px-3">Delete</a>
                        </div>
                    </div>
                <% }); %>
       <% } else{ %>
   <h1 class="text-zinc-500 text-xl ">Not User find yet</h1>
      <% }%>
    </div>
</body>
</html>
